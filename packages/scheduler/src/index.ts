// @wherego/scheduler — PURE TypeScript. No I/O, no Cloudflare bindings, no fetch, no Date.
// Empty for T01 (monorepo scaffold). The Phase 2 target layout (candidates.ts, reachability.ts,
// assign.ts, route.ts, rules.ts, mutate.ts, catchup.ts, simulate.ts — see docs/PLAN.md §2) is
// deliberately not stubbed out here; see docs/plans/00-foundations/tasks/T01-monorepo-scaffold.md.
