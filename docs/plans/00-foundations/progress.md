# Progress — Phase 0 · Foundations

**Plan** [`../00-foundations.md`](../00-foundations.md) · **Spec** `docs/PLAN.md` §2, §3, §9, §10, §11
**Scoped** 2026-07-25 · **Updated** 2026-07-26 · **Status** in progress — 5/21 done

## Exit gate

> **A green production deploy through the full chain, and an unauthenticated app route returning
> 403.** Do not begin Phase 1 until both are true — every later phase deploys through this chain,
> and a authentication hole found in Phase 4 has by then been assumed correct by four phases of
> work.

## Board

`Exec` is `agent` (the harness builds it) or `manual` (a purchase, a console click, or a person —
`/build-task` cannot run it; `/run-plan` halts and hands over the task file's checklist).

| ID | Task | Plan tasks | Deps | Exec | State | Attempt | Latest artifact | Notes |
|----|------|-----------|------|------|-------|---------|-----------------|-------|
| T01 | Monorepo scaffold | P0-02 | — | agent | **done** | 3 | [`validation-03.md`](work/T01-monorepo-scaffold/validation-03.md) | Sets every convention downstream copies. 61 tests, 18 mutations all red. **Must be committed before T10's CI can pass** |
| T02 | Cloudflare account & zone check; app hostname | P0-01a | — | manual | **done** | 1 | [`validation-01.md`](work/T02-cloudflare-account-check/validation-01.md) | **`wherego.storium.work`.** Zone live on Cloudflare NS. Apex already in use → T19's Access app must scope to the subdomain, not the zone |
| T03 | Cloudflare provisioning — confirm plan, D1 APAC | P0-03a | T02 | manual | **done** | 1 | [`validation-01.md`](work/T03-cloudflare-provisioning/validation-01.md) | **Workers Free, nothing purchased.** `database_id` recorded for T04. Bonus: Zero Trust already Active → **T19 needs no purchase either** |
| T04 | `wrangler.toml` — bindings, crons, `[assets]`, `[env.local]` | P0-03b, P0-01b | T01, T03 | agent | **done** | 6 | [`fix-04.md`](work/T04-wrangler-config/fix-04.md) | **Closed on the user's instruction.** Config never had a defect in 6 attempts — `d8e8e3f4…` since attempt 1. 195 tests. **No fresh-agent validation on fix 04** — stated, not glossed |
| T05 | Migration 0001 — the full §3 schema | P0-04 | T01 | agent | **done** | 2 | [`validation-02.md`](work/T05-migration-0001/validation-02.md) | **PASS WITH NOTES**, 2 round trips. 110 tests. **R6 must never be weakened** — 7 defect classes rest on it alone; the reasoning is now a comment on R6 itself |
| T06 | Local loop — `wrangler dev --local` with real D1 | P0-15a | T04, T05 | agent | todo | 0 | — | Gate: every later validation runs through it |
| T07 | `/healthz` and the Hono skeleton | P0-05 | T04, T06 | agent | todo | 0 | — | |
| T08 | Default-deny Access JWT middleware + CI test | P0-07 | T07 | agent | todo | 0 | — | Half the exit gate. Mock JWKS at `tools/mocks/cf-access/` |
| T09 | Seed data — `doctors`, `holidays`, `settings` | P0-17a | T05 | agent | todo | 0 | — | `expected_roster_size` stays NULL until T11 |
| T10 | `ci.yml` | P0-12 | T01, T08, T09 | agent | todo | 0 | — | |
| T11 | The clinic conversation — 個資法 + seven questions | P0-16 | — | manual | blocked | 0 | — | **Clinic.** Book in week 1 — longest lead time; blocks Phases 1 and 2, not Phase 0 |
| T12 | Google Cloud project + Maps ToS caching answer | P0-09 | — | manual | todo | 0 | — | The ToS answer is a schema gate for Phase 1 |
| T13 | LINE channels — production and development | P0-08 | T02 | manual | todo | 0 | — | Webhook path must match T08's allowlist |
| T14 | healthchecks.io checks + `age` backup keypair | P0-10 | — | manual | todo | 0 | — | |
| T15 | 1Password vault, service account, `production` Environment | P0-11 | T03, T12, T13, T14 | manual | todo | 0 | — | Required reviewer is the only human gate before production |
| T16 | `deploy.yml` | P0-13 | T10, T15 | agent | todo | 0 | — | Authoring only; T20 runs it |
| T17 | `backup.yml` | P0-14 | T15 | agent | todo | 0 | — | Decrypt drill is T20 |
| T18 | Bootstrap deploy and bind the custom domain | P0-17b, P0-01c | T07, T15 | manual | todo | 0 | — | `secret bulk` needs a Worker to exist |
| T19 | Cloudflare Access application | P0-06 | T18 | manual | todo | 0 | — | `/healthz` on **bypass**, not allow-everyone |
| T20 | First green production deploy + four negative proofs | P0-13v, P0-14v | T16, T19 | manual | todo | 0 | — | **This is the exit gate** |
| T21 | Preview-version flow and the dev-channel tunnel | P0-15b | T13, T18 | manual | todo | 0 | — | |

## Log

Append-only. One line per state transition. Newest last.

- `2026-07-25` · **phase** · `— → scoped` · 21 tasks from 17 plan tasks; 9 agent, 12 manual. T11 seeded `blocked` (clinic).
- `2026-07-25` · **phase** · board approved · Manual tasks: `/run-plan` halts and hands over the task file's `## Manual checklist`. No change to `CONVENTIONS.md`. `/validate-task` still verifies the *result* where it can be observed (dig the zone, curl for 403, confirm `op://` refs resolve) — never the clicking.
- `2026-07-25` · **T01** · `todo → planned` · [`work/T01-monorepo-scaffold/plan.md`](work/T01-monorepo-scaffold/plan.md) · 4 questions answered. Repo confirmed greenfield — `git ls-files` empty. **Node 22 → 24 across the spec** (`docs/PLAN.md` §11.1/§11.2, `00-foundations.md`, the T01 task file) on the user's decision.
- `2026-07-25` · **T04** · scoping gap fixed · Workers Static Assets (§2, §8, §11.2) had **no owner** in Phase 0 — T04 never mentioned assets and neither did P0-03. T04 now owns the `[assets]` block, +3 criteria. New open question recorded there: worker-first vs assets-first routing, which decides what §9(d)'s "app route returns 403" means for T08 and T20.
- `2026-07-25` · **T01** · `planned → criteria-set` · [`work/T01-monorepo-scaffold/acceptance.md`](work/T01-monorepo-scaffold/acceptance.md) · **agreed and frozen.** 7 config requirements + 10 scenarios; 14 evidence rows, all `unit`/`e2e`, no `inspection`, no `manual`, no mocks. Task criterion 3 (`migrations/` as a workspace member) re-read as tracked-directory + 6 code members, on the record.
- `2026-07-25` · **T01** · `criteria-set → executed` · [`work/T01-monorepo-scaffold/execution.md`](work/T01-monorepo-scaffold/execution.md) · attempt 1, complete. ~30 files, all six root scripts exit 0 (re-run by the harness, not taken from the agent), 41 tests in 3 files, `Date` ban proven both directions with a real eslint probe. 3 plan gaps folded back. **Plan Risk 1 closed** — `pnpm --filter web build` resolves, T16 copies §11.2 verbatim. **T10 gained a criterion**: pnpm 11's build-script approval gate, survivable only because `allowBuilds` is committed.
- `2026-07-25` · **harness** · **one pause per task.** `/run-plan` now stops only at design — questions, plan and criteria presented together for a single confirmation — then runs build → validate → fix → doc straight through to `done` and into the next task's design. Bugs found in validation no longer prompt; fixing them is the run. Halt conditions unchanged, and are failures rather than check-ins. Edited: `run-plan/SKILL.md`, `CONVENTIONS.md`, `CLAUDE.md`.
- `2026-07-25` · **T01** · `executed → bugs-found` · [`work/T01-monorepo-scaffold/validation-01.md`](work/T01-monorepo-scaffold/validation-01.md) · 3 agents, 3 lenses. **B1 HIGH** `globalThis.Date` walks straight past the ban. **B2 HIGH** no app declares a `@wherego/*` dependency, so nothing can import them. **B3/B4 MEDIUM** five script names unguarded; `engines` range vs `.nvmrc` pin. 8 guard mutations run, 7 went red. Coverage gap: Scenario 1 unprovable until the repo is committed — **T10's CI fails until then.**
- `2026-07-25` · **T01** · `bugs-found → fixing` · [`work/T01-monorepo-scaffold/fix-01.md`](work/T01-monorepo-scaffold/fix-01.md) · all 4 fixed, 8 files, 4 regression tests each proven to fail first. Tests 41 → 54. LOW purity finding rejected in writing (faithful to frozen Scenario 6). B4 fixed rather than routed back to design: the contract outranks `plan.md`, which has been corrected.
- `2026-07-25` · **T01** · `fixing → bugs-found` · [`validation-02.md`](work/T01-monorepo-scaffold/validation-02.md) · B1–B4 confirmed fixed and mutation-tested. 5 **new** holes in §2's `Date` rule, none a contract violation: a date library in `devDependencies` (HIGH — pnpm installs devDeps and Wrangler bundles them), `Intl.DateTimeFormat()`, `performance.timeOrigin`, `InstanceType<typeof Date>`. N5 (runtime-computed access) rejected as statically undecidable.
- `2026-07-25` · **T01** · `bugs-found → fixing` · [`fix-02.md`](work/T01-monorepo-scaffold/fix-02.md) · N1–N4 closed in two layers — manifest guard catches declaring a date library, `no-restricted-imports` catches using it. Tests 54 → 61.
- `2026-07-25` · **T01** · `fixing → validated` · [`validation-03.md`](work/T01-monorepo-scaffold/validation-03.md) · **PASS**, round trip 3 of 3. All 8 prior bugs fixed; N5's rejection independently upheld. **No leak** — every banned construct still legal in `domain`, `geo`, `api` and `web`. 18 mutations, none stayed green. Cold CI all six exit 0.
- `2026-07-25` · **T01** · `validated → done` · `doc.md` and `feature.md` **created** (first run). `doc.md`: one line per file across all 34, the stack marked honestly where things are not yet wired, and the `Date` ban documented as the convention it is. `feature.md`: **no user-facing feature exists** — T01 is infrastructure and gets no entry, per the rule. Four enforced invariants recorded instead, since they are the only thing the repo currently does.
- `2026-07-26` · **T02** · handed over (manual) · Two decisions of record, both the user's: **the app hostname is `wherego.<zone>`** — a subdomain, leaving the apex free, per §10.7 step 0 — and **the two Account IDs live in 1Password only** (§10.2), so this repo records only *that they matched*, never the values. **Gap found while scoping the handover: the zone is named nowhere.** `docs/PLAN.md` requires a custom domain in four places (§2 L81, §10.5 L1242, §10.7 L1292, §11.2 L1448) and says the zone is already registered and active, but never says which domain — and neither does any task file. T02's open question is therefore wider than written: it needs the *zone*, not only the hostname label. Awaiting the zone name and the account-ID match before this can go `done`.
- `2026-07-26` · **spec** · **1Password layout changed to match the vault, on the user's decision.** The vault is `Wherego`, one item `credentials`, one **section** per service, `UPPERCASE` fields — so a reference is four segments, `op://Wherego/credentials/CLOUDFLARE/ACCOUNT_ID`, not the three the spec assumed. Rewritten: `docs/PLAN.md` §10.1, §10.2, §10.3 (9 rows), §10.5 (whole block), §10.7 step 7, §11.2's `deploy.yml` (7 refs), §12; `00-foundations.md` P0-11; tasks T13, T14, T15, T19. Zero stale `op://wherego/...` references remain. **A typo was caught before anything referenced it** — the section had been created as `CLOUDFARE`, missing the L; `op://` matches labels literally, so it would have resolved empty at T20 and reported against the workflow step rather than the field. Now `CLOUDFLARE`. **Two pre-existing spec defects closed as a side effect:** §10.5's layout block omitted `BACKUP_AGE_PUBLIC_KEY` and `HEALTHCHECK_PING_URL`, which §10.3 requires — P0-11 and T15 had both been carrying a workaround for that disagreement. All twelve fields are now listed, and the workaround text is deleted rather than left to rot.
- `2026-07-26` · **T02** · `todo → done` · [`work/T02-cloudflare-account-check/validation-01.md`](work/T02-cloudflare-account-check/validation-01.md) · **The app is `wherego.storium.work`.** The zone `storium.work` answers from `eloise`/`kareem.ns.cloudflare.com` with a Cloudflare SOA, so it is Active rather than Pending Nameserver Update. `wherego.storium.work` does not resolve — correct, T18 binds it — which also answers T18's open question that the hostname must not be already routed. **The gap logged above is closed**: the domain is now named in `docs/PLAN.md` §10.7 step 0, and T02's task file carries a `## Decided` section as the single source. Grepped T04, T13, T16, T18 and T19 — all five reference "the T02 hostname" or `APP_HOST`; none names a hostname of its own, so criterion 4 holds without edits. **Finding for T19: the apex is already in use** (Cloudflare proxy IPs), so the Access application must be scoped to `wherego.storium.work` and not to the zone or a wildcard, or whatever else lives on `storium.work` lands behind the clinic's email-OTP policy. **Coverage gap, stated rather than glossed:** the two account IDs were not read back and compared — the evidence is the user's statement that the zone is on the account that will hold the Worker. Sound with one Cloudflare account, and DNS cannot tell accounts apart. If a second account exists, re-check before T03 spends money.
- `2026-07-26` · **spec** · **§2 reversed: WhereGo starts on the Workers Free plan.** The user asked whether Free would do, and the honest answer was that the old §2 claim — Paid is *"a hard prerequisite"*, four scheduler workloads *"impossible at the free plan's 10 ms"* — **was never measured**, since none of those four exist. Checked Cloudflare's live docs rather than the spec's memory: **Durable Objects now run on Free** (the thing that used to force Paid outright), restricted to the **SQLite backend**. The limits that actually bind are **per-invocation, not per-day** — 10 ms CPU, 50 subrequests, 50 D1 queries — so the clinic's small volume does not rescue you from them, which is the crux the user's cost intuition was missing. Three workloads sit near those ceilings: the nightly planner, first-import geocoding (50 subrequests ≈ 50 patients), and §11.3's `age`-encrypted export. Escape hatch, recorded so it is not re-derived: the planner is capped **per doctor**, so it splits per doctor with no data-model change; failing that, Paid is US$5/month, account-wide, no redeploy. **Phase 2 decides it with a number.** Rewritten: `docs/PLAN.md` §2 (the bullet, replaced), §10.7 step 1, §12; `ROADMAP.md` risk register (zone risk **closed** by T02; two Free-plan risks added at Phases 1 and 2); `00-foundations.md` prerequisites + P0-03; tasks T02, T03 (rewritten — **it no longer buys anything**), T04.
- `2026-07-26` · **decision** · **AWS (Lambda + DynamoDB) costed and rejected**, at the user's request, recorded in §2 so it is not re-litigated. It does reach ~US$0/month — Lambda's 1M requests and DynamoDB's 25 GB are perpetual, though new AWS accounts now close at 6 months unless converted. Rejected on three non-price grounds: `PlanCoordinator` has **no drop-in equivalent**, so §6.5's serialization argument is rebuilt from scratch; the SPA and API land on **two origins**, reintroducing CORS and forfeiting the single-Access-application property §9 depends on; and **§3 is relational** — a `schedulable_patients` view and window queries — which is DynamoDB's worst shape. The decisive point is not any of those: **Cloudflare Free is also US$0, with no migration at all.** AWS only becomes interesting if Free provably cannot run the planner, and at that point the fix is US$5 rather than a re-authored 1,545-line specification.
- `2026-07-26` · **T04** · two requirements added ahead of design · **`PlanCoordinator` must be `new_sqlite_classes`, not `new_classes`** — key-value DOs are Paid-only, and the failure is not at config time but at first instantiation (T06 at the earliest, possibly T20). It is also correct on Paid, so it never needs revisiting. **`[limits] cpu_ms` is omitted on Free** — a Paid-only setting, reversing P0-03's "pin it explicitly"; T04 reads T03's recorded plan and leaves a comment naming §2 so the omission is not "fixed" by the next reader.
- `2026-07-26` · **T05** · `todo → planned` · [`work/T05-migration-0001/plan.md`](work/T05-migration-0001/plan.md) · 2 questions answered. **The task's own stated proof was unrunnable**: its `Validation` section says `wrangler d1 migrations apply wherego --local`, but `wrangler.toml` is T04, which is blocked behind manual T03 — so the only unblocked agent task on the board could not be proven the way it was written. Settled on Node 24's built-in **`node:sqlite`** (verified unflagged, SQLite 3.51.2, typed in `@types/node` 24.13.3), asserting against `sqlite_master` after applying the file; **T06 re-applies it against real D1**, and the two gaps — a different SQLite build, and wrangler's own statement splitter meeting a comment-dense file for the first time — are recorded rather than glossed. Rejected `better-sqlite3` (native module, trips the pnpm 11 build-script gate T10 already guards). **`skip_reason` gets no `CHECK`** on the user's decision: §3 states the requirement in a comment, enforcement stays in the application layer, and the migration remains a verbatim copy — adding a constraint later is a full table rebuild under expand-only, so it was now or never. Two things decided rather than asked: `plan_runs`' redundant `UNIQUE(id)` is **carried verbatim** (the spec wins; deviating in migration 0001 puts the database and §3 in disagreement on day one), and the test lives at `tools/guards/schema-0001.test.ts` because `migrations/` is deliberately not a workspace member and `tools/guards/**` is the only vitest include path outside a package `src/` tree — **zero config change**.
- `2026-07-26` · **T05** · `planned → criteria-set` · [`work/T05-migration-0001/acceptance.md`](work/T05-migration-0001/acceptance.md) · **agreed and frozen.** 7 structural requirements + 8 scenarios; 15 evidence rows, all `unit`, **no `inspection`, no `manual`, no mocks** — nothing in this task reaches a network. **Four scenarios assert an absence of over-constraint** — a `missed` attempt plus a live one (§5.1), a `cancelled` attempt plus a live one (§6.4, which §3's comment names but the task file omitted), two `general` visits at `cycle_index IS NULL`, and two `plan_runs` on one `run_date` — each being the case a later reader would "fix" with a table constraint, and each of which the very next commit run would then throw on. Two criteria that read as `inspection` were made mechanical instead: **R6** parses `docs/PLAN.md`, extracts the §3 `sql` fence and asserts every non-empty line appears in the migration **in order**, so a paraphrased comment fails the build and so does a later §3 edit that is not mirrored; **R7** asserts the migration contains none of 身分證號 · 性別 · 主診斷 · 照護階段 · 機構簡稱 · 里 — six columns, never a seventh, enforced in the first artifact that could break it. Two scenarios added beyond the task file's criteria and justified from §3 itself: the `schedulable_patients` filter (soft-deleted and un-geocoded patients excluded — *"the planner reads through this and nothing else"*) and foreign-key rejection under `PRAGMA foreign_keys = ON`, which is what D1 enforces.
- `2026-07-26` · **T05** · **frozen contract amended, on the user's decision** — the only such change so far, recorded here because `CONVENTIONS.md` requires it. **R7 as I froze it was unsatisfiable.** It banned the six excluded CSV column names outright, while R6 mandates §3 verbatim — and §3 L155 is `-- 身分證號, 性別, 主診斷, 照護階段, 機構簡稱 and 里 are NOT read and NOT stored (R13).`, the single occurrence of any of them in the migration, present precisely to **declare** the exclusion. No correct implementation could satisfy both. My defect, found at build: the build agent refused both bad exits — paraphrasing the comment, and special-casing the check to skip that line — and left 6 tests failing with a written explanation, which is exactly what the brief asks for and the reason the "never weaken a test" rule is worth having. **The replacement is stronger, not weaker:** the six names may appear only inside `--` comment lines, never in executable DDL, **and** `PRAGMA table_info(patients)` must match §3's twenty-three columns as a set in both directions. The original string ban protected less than it looked like it did — a real seventh column would be named `national_id` or `diagnosis`, not `身分證號`, and the substring scan would never have caught it. The column-set assertion catches it whatever it is named.
- `2026-07-26` · **T05** · `criteria-set → executed` · [`work/T05-migration-0001/execution.md`](work/T05-migration-0001/execution.md) · attempt 1, complete. 3 files; **93 tests green**, typecheck and lint clean, all re-run by the harness rather than taken from the agent. The migration body is **byte-identical** to `docs/PLAN.md` L143–427, verified by `diff` — not by the agent's assertion that it copied faithfully. **Seven mutations run, every one went red in the right place**: dropping the partial index's `WHERE` clause fires R3, R6 **and Scenarios 2 and 3** — the two absence-of-over-constraint tests doing exactly the job they exist for; a paraphrased comment fires R6, so criterion 9 is a real assertion rather than an inspection; and a `national_id` column added to `patients` fires **R7b alone**, which is the amendment justifying itself, since the substring ban it replaced would have seen nothing. Scenario 4 correctly stayed green under the non-partial mutation — SQLite treats NULLs as distinct in a unique index, so general visits survive it, and the test is right not to claim otherwise. One limit recorded rather than hidden: R7a strips whole `--` lines only, so a banned name in a trailing inline comment would not be caught.
- `2026-07-26` · **T05** · `executed → bugs-found` · [`work/T05-migration-0001/validation-01.md`](work/T05-migration-0001/validation-01.md) · 3 agents, 3 lenses. **The migration is not in question — all four bugs are in the guard that protects it.** Two lenses independently confirmed byte-identity with §3 (one via `awk`+`cmp`, MD5 matching both sides) and drove the schema as a live database: the full missed→re-plan→cancelled→re-plan history coexists, re-proposing a live obligation is rejected and succeeds once marked `missed`, a crashed run retries on the same `run_date`, all five `REFERENCES` bite under `PRAGMA foreign_keys = ON`, and `cycle_index = 0` is correctly treated as present rather than absent. **B1 HIGH** — R6 selects the **first** `` ```sql `` fence in a 1,500-line spec with no anchor to §3; reproduced by inserting an illustrative fence above §3, after which R6 validated the correct migration against a **two-line stub and returned PASS**. That matters because adversarial testing established R6 is doing most of the real work: a `REAL`→`TEXT` change, a dropped `NOT NULL`, a changed `DEFAULT`, an index built on the wrong columns under the right name — every one is caught by R6 and by nothing else. **B2 MEDIUM** — nothing detects an *added* object: an extra column on any of the fifteen non-`patients` tables, an extra index, an extra trigger, or **a second view doing `SELECT * FROM patients`** — the exact thing §3's comment forbids — each leaves the real suite green at 32/32, verified by the harness. **B3 MEDIUM** — §3 contains two duplicate line pairs (`PRIMARY KEY (doctor_id, day)`, `route_km REAL,`), so R6's cursor scan can be absorbed by the surviving copy and then blame an unrelated line. **B4 LOW** — R7a's own rationale comment claims the migration has no inline `--` comments; there are 64. Rejected in writing: `plan_runs.error_text`, boolean `INTEGER`s without `CHECK (x IN (0,1))`, and JSON columns without `json_valid()` are all §3-level and faithfully copied. **Carried forward as spec findings, not T05 defects:** `csv_imports.status` has no `CHECK` while every other status column does, and `NOT NULL` columns accept `''` — both land in Phase 1, which owns the importer.
- `2026-07-26` · **T05** · `bugs-found → fixing` · [`work/T05-migration-0001/fix-01.md`](work/T05-migration-0001/fix-01.md) · **all 4 fixed in one file; the migration was never touched** (sha1 unchanged, still byte-identical to §3, re-verified after every probe). **B1** anchors R6's extraction on the `## 3. Data model` heading and adds two sanity assertions — the block must exceed 200 lines and carry both a start and an **end** sentinel (`CREATE TABLE audit_log (`), so a truncated block cannot pass quietly; proved on a scratch spec copy where the old logic collapses to a 2-line stub and the new one still extracts all 285. **B2** adds a whole-`sqlite_master` object-set assertion (`type:name` tuples, both directions) plus `PRAGMA table_info` column sets for the other fifteen tables. **B3** replaces the cursor scan with contiguous-substring containment and a first-differing-line report; the `plan_days` PK mutation now names `docs/PLAN.md:264` / `migration:126` instead of blaming a line several hundred away. **B4** deletes the false claim and strips trailing comments too. **Fail-then-pass re-run by the harness, not taken from the agent:** all four mutations that previously passed **green at 32/32** now go red. Tests 93 → **110**. Three findings rejected in writing — `plan_runs.error_text`, boolean `INTEGER`s and JSON columns are §3-level and faithfully copied, the same argument that kept the redundant `UNIQUE(id)` and refused the `skip_reason` CHECK.
- `2026-07-26` · **T05** · `fixing → validated` · [`work/T05-migration-0001/validation-02.md`](work/T05-migration-0001/validation-02.md) · **PASS WITH NOTES**, round trip 2 of 3. **B1–B4 all confirmed fixed by independent attack**, including a variant nobody had tried: a **duplicated `## 3. Data model` heading** placed earlier with its own short fence — `findIndex` does take the first heading, but the `>200`-line and dual-sentinel checks then fail loudly, so there is no false pass. The fix's own biggest risk was cleared: all fifteen hand-transcribed `PRAGMA table_info` lists, the 24-object `sqlite_master` set and R7b's 23 columns were re-derived from §3 **by hand** — deliberately not from the migration, which would only have proven the two agree — and every one is correct and load-bearing in both directions. Two closed hypotheses worth not re-testing: an object named `sqlite_autoindex_*` cannot dodge B2a's filter (SQLite refuses any `sqlite_`-prefixed name), and a cross-type name collision is impossible. **The run's most valuable output is the coverage boundary**, now measured rather than assumed: **seven defect classes are caught by R6 and nothing else** — column type, `NOT NULL`, `DEFAULT`, a **widened** CHECK list, an index on the wrong columns under the right name, a narrowed `PRIMARY KEY`, and column reordering. B2a/B2b/R7b never look at type, default, nullability or an index's key list, and Scenario 6 only proves the *old* invalid values are still rejected, never that the allowed set is exactly right. **That reasoning is now a `DO NOT WEAKEN THIS CHECK` comment on R6 itself**, because a boundary that lives only in a validation report is one someone optimises away. **N1 LOW, corrected: the count in B4's replacement comment was wrong — 64 should have been 57, and the error was mine**, from a `grep` that also counted indented whole-line comments; fixed in place since it is comment-only and a wrong number inside the comment written to replace a wrong number is not shippable. `validation-01.md` keeps the 64 as the record of what was found. **N2 LOW, accepted not fixed:** R6's failure *diagnostic* anchors on the first occurrence of §3's opening line, so a duplicate of that one distinctive comment would misdirect the message — the verdict itself comes from `.includes()` and can never be wrong, and the precondition cannot arise without §3 changing first.
- `2026-07-26` · **T05** · `validated → done` · `doc.md` and `feature.md` updated. **`doc.md`**: the Storage line no longer says "no migrations exist" but is honest that nothing *applies* it yet (T04, T06); `migrations/` and `schema-0001.test.ts` gain entries; and the Conventions section loses "migrations are expand-only has no code to point at" — it does now, so the rule is stated with the `schedulable_patients` reasoning attached. Verified both directions: every path named in `doc.md` resolves, and no source file in the tree lacks a line. **`feature.md`**: **still no user-facing feature, and T05 gets no entry** — a schema is infrastructure, and it is not even applied to a database yet. Three enforced invariants added instead, since they are things the repo genuinely does and are checkable: the schema cannot drift from §3, a seventh patient column cannot be added unnoticed (and neither can any table, view, index or trigger), and **the three deliberate absences of constraint are protected** — replacing the partial index with a plain table constraint fails the build.
- `2026-07-26` · **T03** · `todo → done` · [`work/T03-cloudflare-provisioning/validation-01.md`](work/T03-cloudflare-provisioning/validation-01.md) · **Workers Free, and nothing was purchased** — every line on the Subscriptions page is $0.00 or `—`. The reading is **absence-based and said so**: Cloudflare lists Workers Paid as its own `PLANS` entry when held, and the only `PLANS` row is the zone's Free Plan for `storium.work`. That is the same thing as Free, but it is not a positive statement from Cloudflare and the report does not pretend otherwise. **`database_id` = `f5adacb4-abce-41c9-aa82-86dc3b6f8334`**, recorded in T03's task file as the single source; not a secret (§10.4), so it goes in `wrangler.toml` in plain text and **must not** go to 1Password. **Two findings the task did not go looking for.** First, **Zero Trust is already Active on Teams Free Base** — that is what T19's Access application needs, the free tier covers 50 users against a clinic of a handful, so §9's email-OTP design costs nothing and **T19 has no purchase either**; written into T19's task file as a satisfied prerequisite rather than left for T19 to discover mid-build, along with the reminder that the application must scope to `wherego.storium.work` alone because the apex is already in use. Second, **T02's open account risk is closed by construction** — `storium.work` appears under this account's own subscriptions, so the zone and the account holding D1 are demonstrably the same, which is stronger evidence than T02 had, where the match rested on a statement because DNS cannot distinguish accounts. **Coverage gap, stated rather than glossed:** the D1 **name and APAC region were not read back** — wrangler is not installed and the API token that would allow a scripted check is deliberately T15, so both rest on the instruction having been followed. This matters more than the usual attestation gap because **the region is the one irreversible choice in the task**. A name mismatch fails loudly at T06's first `wrangler d1 migrations apply wherego`; the region is not surfaced by that command and should be eyeballed before then.
- `2026-07-26` · **T04** · `todo → planned` · [`work/T04-wrangler-config/plan.md`](work/T04-wrangler-config/plan.md) · 3 questions answered. **Nothing in this plan was recalled — wrangler 4.114.0 was installed in a scratchpad and every claim run**, which is how four of the task file's assumptions turned out to be wrong. **(1) `[env.local]` inherits neither `d1_databases` nor `durable_objects`** — wrangler says so out loud, and `env.ENVIRONMENT` was the *only* binding the local environment had, so T06's "local loop with real D1" would have run against no database. Settled by duplicating both blocks under `[env.local]` and guarding the duplication with a both-directions deep-equal test: §9's "set only in the `[env.local]` block" stays literally true and drift fails the build. **(2) `not_found_handling` does not give SPA fallback when a Worker is present** — `/plan/2026-08-03` reached the Worker, not `index.html`. The fallback needs `binding = "ASSETS"` *and* Worker code calling `env.ASSETS.fetch()`, so it is not the config line the task file assumed. **(3) `run_worker_first` as an array is a trap** — it inverts the default for every path *outside* the list, and under `["/", "/index.html"]` a request to `/healthz` returned `<html>SPA SHELL</html>` with a 200. A Phase 3 route not added to the list would deploy clean and serve the shell. The measured table goes into the file as a comment, and the guard asserts `=== true` **and** `!Array.isArray`. **(4) `wrangler deploy --dry-run` hard-errors twice before it validates anything** — once because `PlanCoordinator` is not exported from the entrypoint, once because `apps/web/dist` does not exist; and any named environment makes a bare dry run emit a "no target environment" warning, so the clean form is `--env=""`. No credentials are needed. **The routing fork is settled: `run_worker_first = true`** (user) — every path invokes the Worker, so §9(d)'s *"an unauthenticated request to an app route returns 403"* is true for `/` and for every asset, and T08 and T20 inherit a sentence with no caveat. Assets-first was cheaper and lost because `GET /` returning 200 forever would need explaining to every future reader of the exit gate; the price is one Worker invocation per static file against Free's 100k/day, three orders of magnitude of headroom. **Five hand-offs recorded rather than left to be rediscovered:** T06's command is `wrangler dev --local --env local`; T08 must default-deny asset requests rather than allowlist them, and the allowlist stays at exactly two entries; T10 must run `pnpm build` **before** the dry run; T16 inherits §11.2's bare `wrangler deploy` and its warning, flagged not changed because §11.2 is specification; T20 may name any path but the two bypasses. Also decided: `wrangler` goes on `@wherego/api` (§11.2 runs `pnpm --filter api exec wrangler`), `workerd: true` joins `allowBuilds`, and the guard parses TOML with `smol-toml` on `@wherego/tools`.
- `2026-07-26` · **T10** · spec drift fixed while recording T04's hand-off · **T10's acceptance criteria still said "Node is 22".** The spec moved to Node 24 at T01 — `docs/PLAN.md` §11.1/§11.2, `00-foundations.md`, `.nvmrc` and the root `engines` range were all updated then, and this one line was the last place still disagreeing. It would have shipped a CI workflow pinning a Node the repo's own `engines` range forbids, and the failure would have arrived as a `pnpm install` refusal in the first CI run rather than as anything naming the version. Corrected in place with the reason attached.
- `2026-07-26` · **T04** · `planned → criteria-set` · [`work/T04-wrangler-config/acceptance.md`](work/T04-wrangler-config/acceptance.md) · **agreed and frozen.** 13 requirements + 8 scenarios; 18 evidence rows, all `unit`/`e2e`/`integration`, **no `manual`, no `inspection`, no mocks** — nothing in this task reaches a network and the dry run needs no Cloudflare credentials. **One criterion is deliberately *not* claimed: an unauthenticated request returning 403.** T04's stub Worker forwards every path to `env.ASSETS.fetch()`, so worker-first and assets-first produce byte-identical responses on every path — measured with both configurations side by side, not assumed — which means `run_worker_first = true` is a static assertion here and a behavioural one at **T08**, whose task file gained the matching criterion. Four scenarios exist only to make guard mutations fail: the array form of `run_worker_first`, `new_classes` for `new_sqlite_classes`, a re-added `[limits]`, and a §10.3 secret name in the file. **Scenario 6 asserts a *failure*** — the dry run must exit non-zero when `apps/web/dist` is absent, which is the good case, because an assets directory resolving to somewhere empty would deploy clean and serve nothing. Mutations are applied to a copy and both `wrangler.toml` and `src/index.ts` are checksummed before and after.
- `2026-07-26` · **T04** · `criteria-set → executed` · [`work/T04-wrangler-config/execution.md`](work/T04-wrangler-config/execution.md) · attempt 1, complete. 8 files; **155 tests green** (110 → 155), typecheck and lint clean, all re-run by the harness rather than taken from the agent. **Both dry runs exit 0 with stderr at 0 bytes** — `--env=""` lists `DB`/`PLAN_COORDINATOR`/`ASSETS` and reads 3 assets, and `--env=local` lists all **four** including `ENVIRONMENT ("local")`, which is the direct evidence that the `[env.local]` duplication is right: an un-duplicated block reappears as a wrangler warning naming the missing key. Credentials were unset for both, confirming the dry run stays credential-free for T10. **Two planning defects folded back into `plan.md`.** First, **T01's `scaffold.test.ts` asserted `apps/api/wrangler.toml` does not exist** — a boundary check on T01's own out-of-scope list, which T04 legitimately crosses; the agent dropped that one expectation, kept the `.github` half (still correctly absent until T10) and left a comment saying the boundary moved rather than vanished. Not a weakened test: the thing it protected has happened, on schedule, in the same phase. Second, **R10 and R5 ban literal strings, so a comment explaining "not `new_classes`" is itself a match** — reworded the prose rather than special-casing the check, which is the right way round and the second time this repo has hit it, after T05's contract amendment. **One thing the harness observed that the agent did not report:** several of the guard's `Scenario N: … would be caught` tests are **tautologies** — they build the mutated value in the test body and assert it differs from the expected one, which is true by construction and would stay green if the real assertion above were deleted. Deliberately not fixed here, because `/build-task` does not fix and Agent C is briefed to find exactly this on its own merits; recorded in `execution.md` so it reaches triage with a reproduction if validation misses it.
- `2026-07-26` · **T04** · `executed → bugs-found` · [`work/T04-wrangler-config/validation-01.md`](work/T04-wrangler-config/validation-01.md) · 3 agents, 3 lenses. **The config is not in question — all six bugs are in the guard that protects it**, the same shape T05's first validation had. Two lenses drove it as a live system: both dry runs exit 0 with **stderr measured at 0 bytes** and `--env=local` shows all four bindings with no non-inheritance warning; `/` and `/plan/2026-08-03` return bodies **sha256-identical** to the built `index.html` and a hashed bundle comes back as `text/javascript`; **the Durable Object actually instantiates** (`idFromName → get → fetch` returned the 501 stub, which is the specific key-value-backend failure `new_sqlite_classes` exists to avoid, now disproved by execution rather than by reading the key); and **D1 is a live database rather than a name** — `SELECT 1` returned through Miniflare while `sqlite_master` held only `_cf_METADATA`, confirming migration 0001 is correctly not applied yet. **28 mutations against a full repo copy running the real `pnpm test`; 26 went red**, each named by the assertion that should have caught it, including all six `[assets]` fields, both directions of `[env.local]` drift, and `workers_dev` as the *string* `"false"`. **B1/B2 MEDIUM** — Scenarios 4 and 7 require the failure *message* to explain itself and it does not: the array form reports a bare `expected [ '/api/*', '/healthz' ] to be true`, which leaves the reader free to try a *different* array — the one repair that keeps the suite red and the Worker unreachable. **B3 MEDIUM** — six `Scenario N` tests are tautologies that build the mutated value in their own body; found by three independent routes, and bounded rather than assumed: the adversarial lens proved every *primary* assertion still catches every mutation, so the defect is that the companions would stay green after the assertion beside them was deleted. **B4 MEDIUM, found by the harness at triage and by no agent** — R11's Scenario 8 test **cannot fail**: `findEnvironmentLocalAssignments` always returns the real `wrangler.toml`, so both `matches.length >= 1` and the `|| m.endsWith('wrangler.toml')` disjunct are unconditionally true, and deleting the entire `extraFiles` loop leaves it green. Worse than B3, because it is the only test standing behind a §9 security property — T08's production bypass is gated on exactly this value. **B5/B6 LOW** — R11's scan skips any file outside an extension allowlist while the criterion says "nothing else in the repository"; and the binding set is open, so a `[[kv_namespaces]]` block passes despite §2's "No R2, no KV, no Queues, no Workflows". B6 is promoted to a fix rather than deferred because this guard is the only artifact that will ever read this file, so a deferral would have no destination. **Three findings rejected in writing:** an `[env.production]` block is inert (top level *is* production, by a recorded decision, and nothing selects that name); a present-but-empty `dist` passing the dry run is wrangler's behaviour and is already named in the frozen contract's own Scenario 6 commentary — **carried to T10**, whose dry-run step should assert the asset count is greater than zero rather than only that the command exits 0; and the hand-triggered cron's opaque 500 is correct, since `scheduled()` does not exist until Phase 3 and cron firing is explicitly not required. **Coverage gap, unchanged and unclosable here:** `run_worker_first = true` still has no behavioural proof — the end-to-end lens confirmed by measurement what the plan predicted, that the stub Worker makes worker-first and assets-first byte-identical on every path. Closes at T08.
- `2026-07-26` · **T04** · `bugs-found → fixing` · [`work/T04-wrangler-config/fix-01.md`](work/T04-wrangler-config/fix-01.md) · **all 6 fixed in one file; the config was never touched** — both sha256 values unchanged and re-verified after every probe. Tests 155 → **168**. **B1–B4 were one defect, not four:** the guard's assertions were inline `expect()` calls against a single module-level parse, so a mutation test had nothing to exercise and fell back to asserting something trivially true in its own body. Closed by extracting a pure `findViolations(config, rawToml)` returning self-explanatory strings; every scenario test now mutates the real TOML, re-parses it and runs the same code path the guard runs. **The first pass reported B1 and B2 fixed and they were not** — the logic was right but the output was `expected [ Array(1) ] to deeply equal []`, because vitest collapses a nested array and the violation text never reached the terminal. For a bug whose whole subject is the failure *message*, that is not a fix. Caught by the harness re-running the reproduction rather than believing the report, and closed by passing the joined violations as vitest's message argument at all 17 call sites. **Second time in this task a claim survived until someone ran the thing**, after the task file's four wrangler assumptions. **Fail-then-pass re-run by the harness, not taken from the agent:** loosening the rule *inside* `findViolations` now turns Scenario 4 red (1 failed / 57 passed) where it previously left every scenario green; neutering the roots parameter turns Scenario 8 red, the test that could not fail at all before; a `.sh` file carrying `ENVIRONMENT="local"` — invisible to the old extension allowlist — turns R11 red; and `[[kv_namespaces]]` now fails quoting §2's "No R2, no KV, no Queues, no Workflows" and naming the unexpected type. **One decision flagged rather than buried: `docs/` is excluded from R11's widened scan.** Scanning it would fail against the real repo, because the task file, plan, contract, validation report and T08's task file all quote `ENVIRONMENT = "local"` verbatim as exposition, which is this repo's own convention. The property that matters — no *executable or config* file but `wrangler.toml` sets it — still holds, confirmed by grep, but it narrows R11's literal "nothing else in the repository" and that narrowing is now in the record rather than only in a comment.
- `2026-07-26` · **T04** · `fixing → bugs-found` · [`work/T04-wrangler-config/validation-02.md`](work/T04-wrangler-config/validation-02.md) · round trip **2 of 3**. **The fix holds and the config has now survived two full rounds without a defect** — all six bugs independently reproduced as fixed, **all 28 mutations from round 1 still red** (so the 45 → 58 refactor dropped nothing, which was the specific risk this round was dispatched to check), and **all three rejections re-argued independently and upheld**, one of them better than my original: an empty-but-present `dist` is a *third state* that neither Scenario 1's `Given` ("has been produced by `pnpm build`") nor Scenario 6's ("does not exist") covers. **Two things nobody had proven before:** the Durable Object's SQLite storage is genuinely **disk-backed** — two keys written, the process tree killed, restarted from the same `--persist-to`, both read back with a changed in-process instance id, and a per-DO `.sqlite` file on disk — so `new_sqlite_classes` does what §2 claims, established in Phase 0 rather than discovered in Phase 3; and a **bare `wrangler deploy --dry-run` selects production**, its binding table carrying no `ENVIRONMENT` row, which is the direct evidence for the plan's "a missing `--env` must never fall back to local" decision. **Five new bugs, every one in the guard.** **B7 MEDIUM** — `[env.staging.vars] ENVIRONMENT = "local"` appended to the config leaves all 58 green, because R9's deep-equal is hardcoded to `env.local` and the scan records **one match per file**, so a second illegitimate occurrence inside a file that already legitimately matches is invisible; the realistic path is nobody being malicious, just someone starting `[env.staging]` from a copy of the local vars block. **B8 MEDIUM** — R11's pattern requires a *quoted* value, so unquoted dotenv and YAML forms are missed. **B9 MEDIUM** — R13's three assertions are bare substrings over the whole file, so a `PlanCoordinator` returning 200 with `env.ASSETS.fetch(request)` in an `if (false)` branch passes as long as the literal strings appear anywhere, a comment included; **corroborated from a second angle** by the regression lens finding `/export\s+class\s+PlanCoordinator/` has no word boundary, so renaming the class to `PlanCoordinatorX` also stays green. **B7 and B8 were reported HIGH and I downgraded both, saying why rather than deferring:** R11 as a statement about the repository is *true today* — confirmed by grep — so what fails is the evidence, not the criterion, which `CONVENTIONS.md` calls MEDIUM; and B8's two named vectors reach nothing, since `.dev.vars` is gitignored and never uploaded and a GitHub Actions `env:` block never reaches the Worker. **B10/B11 LOW** — `workers_dev` is not re-checked per environment (not required by R2's wording, but §9(b) makes it load-bearing, and it is the same one-line family as B7), and Scenario 7's cron mutation hardcodes the file's comment text so an unrelated comment edit makes its `.replace()` a silent no-op. **One rejection:** verifying that R6's `08:00`/`07:00` comments state the *correct* time would mean reimplementing UTC→Taipei conversion inside a test — a second implementation of logic, for a comment. **Six hand-offs from the behaviour lens**, the sharpest being that **T10's asset count must be asserted `> 0` and never compared for equality** — measured three times with the cache cleared, a non-empty `dist` is over-reported by a small constant (2 files → "3", 1 file → "2", 0 → "0") — and that even a `> 0` check misses a `dist` built **without a root `index.html`**, which passes the dry run and then 404s on `/` and every unmatched path.
- `2026-07-26` · **T04** · `bugs-found → fixing` · [`work/T04-wrangler-config/fix-02.md`](work/T04-wrangler-config/fix-02.md) · **B7–B11 all closed in the guard; the config was never touched** across any of the three rounds, both sha256 values unchanged and re-verified after every probe. Tests 168 → **181**; the count has risen at every step — 39 → 45 → 58 → 71 in this file — and has never fallen. **Six defect conditions reproduced red by the harness rather than taken from the agent**, including the one I expected to be refused: `env.ASSETS.fetch(request)` moved into an `if (false)` branch. B7 now asserts that **exactly one environment may set `vars.ENVIRONMENT`, and any other environment setting the key at all — whatever its value — is a violation**, because T08's bypass is gated on the value rather than the environment's name; the repo scan counts **occurrences** rather than files, so a second assignment inside a file that already legitimately matches is finally visible. **A defect the fix created and closed itself, worth the record:** counting occurrences immediately broke R11 against the *correct, untouched* file, because `wrangler.toml`'s own explanatory comment quotes `ENVIRONMENT = "local"` verbatim as prose. Closed by stripping comments before counting — and that is now the **third** time in this task a literal-string rule has met a file that documents itself, after R5's `new_classes` and R10's secret names. Three occurrences is a pattern rather than three accidents, and it is written into the boundary comment. **One limitation stated rather than hidden:** B9's dead-branch detection strips only literal `if (false) { … }` blocks by brace matching — it catches the named reproduction and nothing shaped differently, and a real answer needs a parser, which would be a worse decision than the gap for eight lines of stub code **T07 replaces wholesale**. **The durable output of all three rounds is the coverage boundary now sitting at the top of the guard**, in the style of `schema-0001.test.ts`'s `DO NOT WEAKEN THIS CHECK` block and measured across the rounds rather than asserted: `run_worker_first` has no behavioural proof here and cannot until T08; R6 checks a cron comment is *present*, never that its stated clock time is *correct*, with the reason attached so the rejection is not re-litigated; `docs/` is excluded from R11 and comments are stripped inside every scanned file, with the argument for both; and an empty-but-present `dist` passes, T10 checks `> 0`, and **even `> 0` misses a `dist` with no root `index.html` — which nothing catches yet**.
- `2026-07-26` · **T04** · `fixing → blocked` · [`work/T04-wrangler-config/validation-03.md`](work/T04-wrangler-config/validation-03.md) · **round trip 3 of 3 — the limit. Escalated rather than fixed, per `CONVENTIONS.md`.** **The configuration is correct for the third round running** and has never been touched since attempt 1: the sign-off lens drove **every** R1–R13 and Scenario 1–8 independently — credential-free dry runs at 0 bytes of stderr, a live `wrangler dev` returning byte-identical deep links, `dist` moved aside for the loud failure — and returned **met, in full**; the regression lens re-ran **34 mutations**, all red, and re-argued all four rejections, upholding each. **Every round has closed its bugs and produced new ones in the same family, and the family has one cause: the guard is doing static analysis of TypeScript and arbitrary text with regexes.** Round 3's crop: a `//` inside a URL blanks a real `ENVIRONMENT = "local"` on the same line; `/* */` stripping is applied to `.sh` files where those characters are ordinary text, deleting a live assignment; symlinked files are skipped because a symlink `Dirent` is neither `isFile()` nor `isDirectory()`; **`501` matches inside `1501`**, so a class body genuinely returning 200 passes R13 — B9's word-boundary fix was applied to the class name and not to the number one line away; and a default export that never calls `env.ASSETS.fetch()` passes because a decoy *string literal* contains the text. The same blindness fires in reverse, deleting compliant code and failing it. **The harness found something worse than any single bug while reproducing two of them:** both went red — *for the wrong reason*. The failing test was **B11's drift diagnostic**, not R13; the R13 assertions stayed green exactly as reported. **The red/green signal on `src/index.ts` is currently dominated by "did you edit the exact lines my fixtures quote" rather than "is the code correct"** — invisible until someone changes that file for an unrelated reason, which is precisely what T07 will do. **Two sign-off findings judged against the agent rather than deferred to it:** its Hono/T07 finding is **not a bug** — R13 is a claim about *the stub*, the contract says so, and T07 replacing the stub is the same lifecycle event T04 already handled when it retired T01's "`wrangler.toml` must not exist" assertion, so it belongs in T07's task file; and its complaint that banning *any* `ENVIRONMENT` value in any other environment exceeds R11 is **rejected in writing**, because §9 calls the bypass "a production landmine unless it is tested" and a guard that makes a human justify a new `ENVIRONMENT` var is doing its job. **Nothing that ships is at risk** — R13's real proof is Scenario 5's live deep-link request, a missing export fails Scenario 1 loudly, and every value reaching the deployed Worker arrives via `[vars]` or `secret bulk`, neither of which is a `.sh` file or a symlink. What is imperfect is the guard's ability to detect a *future* accident. **Recommendation: replace the text matching rather than patch it a third time** — a behavioural unit test for R13 (import the module, assert the stub returns 501, call the handler with a fake `ASSETS` and assert delegation) is ~15 lines, immune to all three of this round's R13 findings by construction, and lets the brace matcher, the dead-branch heuristic and most of the comment stripping be **deleted**. Smaller guard, tests more, and it answers the sign-off lens's overshoot finding too. **That is a design change, so it goes to `/design-task`, not to a fourth fix agent.**
- `2026-07-26` · **T04** · `blocked → planned` · revision 2 · [`work/T04-wrangler-config/plan.md`](work/T04-wrangler-config/plan.md) · previous plan preserved at [`plan-v1.md`](work/T04-wrangler-config/plan-v1.md) · **the escalation is resolved by a design change, which is where it was sent.** The user chose to **replace the text matching rather than accept its limits or strip it back**, over the two alternatives offered — accept and document the false negatives, or downgrade R13 to Scenario 5's integration evidence. **Nothing about the configuration changes**: `wrangler.toml` and `src/index.ts` are correct, met every criterion under three independent sign-offs, and are byte-identical to attempt 1; the revision explicitly forbids touching either. **R13 becomes a behavioural unit test at `apps/api/src/index.test.ts`** that imports the module, asserts `new PlanCoordinator().fetch()` is a 501, and calls the handler with a `vi.fn()` ASSETS binding to assert it both delegates and returns what came back. B15 (`501` inside `1501`), B16 (a return matched inside a string literal) and N1 (compliant code deleted by the dead-branch heuristic) are impossible against it **by construction — there is no text to match**, and the `if (false)` case that needed a brace matcher plus a heuristic needs nothing at all, because dead code does not run. It also answers the sign-off lens's overshoot finding for free: `export default app` gives an object with a `fetch` method, so the test shape survives T07's Hono rewrite where revision 1's export-shape regex did not. **R11 becomes a parse of every wrangler config in the tree** — walk `vars` and every `env.<name>.vars` on the parse tree, assert exactly one sets `ENVIRONMENT`, that it is `env.local`, and that the value is `"local"`. Comments stop being a problem because comments are not parse-tree nodes, which retires the trap that has now sprung **three separate checks** in this task; the quoting cases (B8) stop existing because a parser returns a value; and `docs/`'s exclusion, the UTF-8 sniff and the symlink hole (B14) all disappear with the filesystem walk. **Seven helpers and two mutation fixtures are deleted** — `stripCodeComments`, `findMatchingCloseBrace`, `extractBalancedBlock`, `findExportedClassBody`, `findDefaultExportFetchBody`, `stripDeadFalseBranches`, `stripScanComments`, plus the two fixtures quoting literal lines of `src/index.ts`. **Those fixtures are the drift diagnostic that fired instead of R13 in round 3**, and after this the guard never reads that file, so its checksum comes out of the hygiene test too: a checksum of a file the test has no relationship to is not hygiene. **The guard gets smaller and proves more**, which is the whole argument for choosing a redesign over a fourth fix agent. **Two contract amendments, approved with the decision and applied to the frozen `acceptance.md` under a dated Amendment block:** R11 narrows from *"nothing else in the repository"* to *"no other wrangler configuration"*, and evidence row 13 moves file. **The narrowing is honest, not a weakening** — validation 02 established *by measurement* that only `[vars]` and `wrangler secret bulk` put a value on the deployed Worker, so the dropped vectors (`.sh` shell variables, GitHub Actions `env:` blocks, `docs/` prose) **cannot reach it**, while the one that can and never was covered is now **T16's criterion**: the `secret bulk` payload must contain no key named `ENVIRONMENT`. **Three design decisions made rather than asked, each with its alternative recorded:** the test lives in `apps/api` and not `tools/guards` because `tools/tsconfig.json` is `types: ["node"]` and adding Worker globals there would pollute five unrelated guards, while `apps/api` already has the right types and `vitest.config.ts` already includes `apps/api/src/**/*.test.ts` — **a path nothing has used yet, so this task sets the colocated-test convention**; `@cloudflare/vitest-pool-workers` is rejected as a new dependency plus a vitest project split to prove eight lines of stub, when validation 02 already proved the real runtime behaviour under Miniflare; and the TOML↔module name agreement gets **no fourth implementation** — `wrangler deploy --dry-run` already closes it in both directions and says so by name, which is Scenario 1 and has passed every round. That last one goes in the coverage-boundary comment rather than being papered over. **Two hand-offs written into the task files rather than left to be rediscovered:** T16 gains the `secret bulk` criterion above, and T07 is told to **rewrite `index.test.ts`, not delete it** — the `PlanCoordinator` 501 stays true through T07 because the real DO is Phase 3, and the delegation assertion becomes a claim about the catch-all route. T07 also inherits a **conflict it must resolve rather than inherit**: its own criterion says an unknown path returns 404, while T04's proven Scenario 5 says an unknown path returns `index.html` with a 200. Both cannot be true; `/design-task` must ask rather than pick.
- `2026-07-26` · **T04** · `planned → executed` · attempt 4 · [`work/T04-wrangler-config/execution.md`](work/T04-wrangler-config/execution.md) · **the redesign, not a fourth fix.** Typecheck and lint clean; **tests 181 → 174, and the fall is the deliverable** — eleven text-matching tests deleted (R13's 3 → 2, B9's 3, B8's 5) against four added, with every other describe block untouched. B8's five tested quoting forms that **cannot occur** once a parser returns the value. Seven helpers and two mutation fixtures are gone, including the two that quoted literal lines of `src/index.ts` — **the drift diagnostic that fired instead of R13 in round 3**; the guard no longer reads that file at all, so its checksum came out of the hygiene test too. **Both config files are byte-identical to attempt 1** and were re-checksummed after every probe. **Eight mutations run by the harness, all red, three of them the harness's own rather than a re-run of the agent's homework:** a real `wrangler.jsonc` planted at `apps/probe/` (proving the extension set works end to end, not only against the synthetic fixture); a **top-level** `[vars] ENVIRONMENT = "local"`, which is the shape that would actually ship the bypass since the top level *is* production; and `env.local`'s assignment **deleted entirely**, exercising the `legitimate.length !== 1` branch that nothing else reached. Every failure names the offending environment **and** file — B1/B2's defect, not recurred. **The result that matters most is not any single mutation but that each one failed on the assertion that owns it.** Round 3's worst finding was that reproducing a bug turned the suite red *for the wrong reason*, a drift diagnostic firing while R13 stayed silently green; that is now structurally impossible on `src/index.ts`. **The `if (false)` dead branch — the case that cost revision 1 a brace matcher plus a heuristic and still had a false-positive twin — now fails on the ordinary delegation assertion with no special handling whatsoever, because dead code does not run.** **Four decisions beyond the plan, two folded back as changes 15 and 16:** `B7`'s block had to be rewritten against the new pure functions, and **R9 had to be separated from R11** — without the split R9's test would have gone vacuous once the cross-check moved, which is exactly B4's fails-for-nothing shape reappearing in a new place. The other two are mechanical: `./index` not `./index.ts` (TS5097), and `Fetcher` as an ambient global rather than a named export. **Two things the harness observed that the agent did not report.** `B7`'s three tests fail with an opaque `Invalid TOML document: trying to redefine an already defined table or value` — not their own assertion — when the real file already contains `[env.staging]`, which is why the staging mutations show 5 red and the top-level one shows 2. Judged **LOW with the reasoning stated rather than assumed**: it is the same *family* as round 3's finding but not the same *defect*, because there a wrong-reason failure **masked** a check that had stayed green, whereas here R11's own test fires correctly on the identical mutation — noise on top of a correct signal, not a substitute for a missing one — and the fragile state is unreachable in a green tree, since any config that triggers it is one R11 already rejects. Separately, **a hand-written JSONC comment stripper was introduced**, which is narrow and argued for at its definition but is the same *technique* three rounds were spent removing; no `wrangler.jsonc` exists in this repo, so it is dead against the real tree, and its realistic failure mode is a **loud** `JSON.parse` error rather than B12/B13's silent line-blanking. Both left for validation to judge on their merits with reproductions attached, rather than fixed by the skill that is not allowed to fix.
- `2026-07-26` · **T04** · `executed → bugs-found` · [`work/T04-wrangler-config/validation-04.md`](work/T04-wrangler-config/validation-04.md) · **round trip 1 of 3 — the counter reset when the escalation went back to `/design-task` and the contract was amended.** 3 lenses. **The redesign did what it was chosen to do, and the adversarial lens proved it rather than asserting it:** it re-checked **all sixteen prior bugs and N1 independently**, and the entire R13 text-matching family — **B9, B15, B16, N1** — is now **moot by construction rather than by patching**. A decoy `1501`, a decoy string literal and a genuine `if (false)` dead branch were each re-attacked; every one failed red **on the assertion that owns it**, with no special handling. B8 cannot recur because "quoting" is not a concept when reading a typed value off a parse tree; B13 cannot recur because `.sh` and `.env` files are never opened. **`assertMutated`'s masking failure — round 3's worst finding, where reproducing a bug turned the suite red for the wrong reason while R13 stayed silently green — is structurally impossible now**, because no fixture quotes `src/index.ts` any more. And the `docs/` exclusion round 2 needed is gone **and did not need replacing**: the new design only opens files literally named `wrangler.toml`/`.json`/`.jsonc`. **The behaviour lens found nothing and proved two things nobody had.** It instantiated the Durable Object **through a real Miniflare DO namespace** — transpiled the real `index.ts`, loaded it as an actual worker script with a genuine SQLite-backed `PLAN_COORDINATOR` binding, namespace → stub → `fetch()` → **501** — closing the gap between the unit test's bare `new PlanCoordinator()` and what the runtime produces, which was the specific question the redesign raised. And **both it and the adversarial lens verified the coverage boundary's central claim by measurement**: removing the `export` keyword makes the dry run fail with *"Your Worker depends on the following Durable Objects, which are not exported in your entrypoint file"*. That check is the stand-in for the TOML↔module cross-check I refused to give a fourth implementation, so it needed to be true rather than assumed. Scenario 5 was verified by **sha256** against the built files, not by eye. **Five bugs, every one in the guard; the config is unchanged and correct for the fourth round running.** **B17 MEDIUM, found independently by two lenses with different fixtures** — the JSONC comment stripper has a **silent** false-negative mode: a `/*` inside one string value and a `*/` inside another are matched as a block comment spanning between them, deleting the whole `vars` object, and **the remainder is still valid JSON**, so `JSON.parse` succeeds and R11 reports no setter at all. Planted at `apps/probe/wrangler.jsonc`, **all 62 guard tests passed with a rogue top-level `ENVIRONMENT = "prod"` invisible**. **This refutes a claim the harness itself made in writing** — `execution.md` argued the realistic failure mode was a *loud* parse error and used that to justify leaving it for validation; that justification was measurably wrong, and the error belongs in the record rather than being quietly dropped. MEDIUM rather than LOW because R11's whole purpose is to be the backstop the day someone adds a `wrangler.jsonc`, and on that day it falls to an ordinary two-string layout rather than an adversarial trick — same mechanism and same class as B12/B13. **B18 MEDIUM** — R13's *"a comment naming Phase 3 as its owner"* clause **has never been tested, in any revision**, not by revision 1's three regexes and not now; the comment is present so the system is compliant, but Evidence row 13 claims `unit` while that clause is `inspection` in practice, and **eleven tests were deleted last round without anyone noticing it had no test to lose** — with T07 rewriting that file next, which is exactly when an untested comment clause disappears. **B19 LOW** — B14's symlink hole is unfixed and carried into the new walk; one genuinely new fact is that an infinite symlink loop is **impossible today precisely because symlinks are skipped**, so any fix must not reintroduce it. **B20 LOW** — a `//` inside an ordinary URL field crashes with an opaque `SyntaxError` instead of Scenario 8's promised *"fails, naming that file"*; same root cause as B17. **B21 LOW** — `B7`'s tests fail with an opaque TOML redefine error rather than their own assertion when the real file already has `[env.staging.vars]`; the adversarial lens sharpened this usefully, showing the collision is on that exact table *path* and that R11's real check still fires when the mutation is expressed as an inline table, and **agreed with the LOW the harness had already argued** — a future test-authoring fragility that will confuse T18/T20, not a masked gap. **Four things rejected in writing**, so the next round does not re-report them: the unit test's `toBe` identity assertion proves a property that only exists in a shared JS heap and is correctly paired with Scenario 5 rather than replaced by it; `/plan%2F2026-08-03`'s 307 is unchanged, downstream of the Worker, and already carried to T08; a throwing ASSETS binding producing a 500 is not a T04 defect when no error-handling task exists and T07 replaces the handler; and **`scaffold.test.ts` failing inside a `.git`-less copy is a copy artifact, not a defect** — though it is worth T01 knowing that a guard shelling out to `git check-ignore` is not portable to a detached copy, which is how every validation in this harness runs.
- `2026-07-26` · **T04** · `bugs-found → fixing` · [`work/T04-wrangler-config/fix-03.md`](work/T04-wrangler-config/fix-03.md) · **all five closed; both config files untouched for the fifth straight attempt**, sha256 re-verified after every probe. Tests 174 → **184**, nothing removed, skipped or loosened. **B17 and B20 were fixed by deletion, which was the instruction rather than the agent's discretion:** `stripJsoncComments` is gone, not improved, because three rounds were spent removing hand-rolled comment stripping from this file and B17 was it growing back — a cleverer version is the same defect with a longer fuse. `readWranglerConfig` is now three lines: `.toml` → smol-toml, `.json`/`.jsonc` → plain `JSON.parse` on raw text with **no preprocessing whatsoever**. The insight that makes it work is that **there was never anything to strip** — `/*`, `*/` and `//` inside a quoted string are just characters, and the only reason they were dangerous is that something was looking for them. The second half matters as much: **a parse failure, for any extension, is now a NAMED violation rather than a thrown exception** — *"R11 could not read …/wrangler.jsonc — … An unparseable wrangler configuration can never be silently treated as one that sets nothing"* — so the scan supports the JSON that `JSON.parse` supports, not JSONC, and says so on the check rather than leaving it to be discovered. **B18** now asserts the Phase 3 comment from `index.test.ts` by raw text, which is the right instrument for a claim about a comment (R6 already does exactly this for the cron lines) and whose alternative is a brace matcher — the thing revision 2 deleted. **B19** follows a symlinked *file* via `statSync` but deliberately never recurses into a symlinked *directory*, because validation-04 recorded that loops are impossible today only because symlinks were skipped outright; the residual limit is **named in the coverage boundary rather than implied away**. **B21** asserts the probe environment name is free before mutating, so the failure lands where the collision is. **Seven probes re-run by the harness against the REAL repo-wide scan** rather than the guard's own temp-directory fixtures — the path a genuine accident would take: B17's exact fixture caught as an ENVIRONMENT violation (it is valid JSON throughout, so it parses and `vars` survives); a jsonc with real comments caught as the named unparseable violation; a symlinked `wrangler.toml` pointing outside the tree caught and named; **two nested self-referential directory symlinks completing in 1 second**; a dangling symlink not throwing; the sole Phase 3 comment removed going red; and `[env.staging.vars]` appended failing on B7's **own named message** instead of a TOML redefine error. Every probe removed and both checksums re-verified. **A false alarm of the harness's own, recorded because the lesson is the point:** the first B18 probe reported PASS and the fix was fine — `s/is Phase 3\./…/` never matched, because in the source `is` ends one line and `Phase 3.` begins the next behind a `// `, so **the mutation silently did nothing and 184 tests passed on an unchanged file.** Caught by checking the mutation had applied before believing its result. That is B11's `assertMutated` class — *a mutation that quietly fails to mutate* — this time in the harness's own probe rather than in the guard, which is a fair reminder that the diagnostic exists because the failure mode is easy to hit. (A second, duller one: `timeout` does not exist on macOS, so the loop probe exited 127 and tripped an `|| echo HUNG` fallback.) **One limitation stated rather than hidden:** B18's assertion matches `Phase 3` anywhere in `index.ts`, not specifically inside the class body, because scoping it there needs a brace matcher — precisely what produced B15, B16 and N1. Whole-file is the right trade for a comment check and it is the same scope R6 uses.
- `2026-07-26` · **T04** · `fixing → bugs-found` · [`work/T04-wrangler-config/validation-05.md`](work/T04-wrangler-config/validation-05.md) · **round trip 2 of 3.** 3 lenses; **two of them signed off clean.** **The regression lens found no regressions and nothing dropped** — it re-checked all twenty-two prior items (B1–B21, N1) **by reproduction rather than by reading** and re-ran a **28-mutation corpus** against the real files: **zero came back green, and zero came back red for the wrong reason.** That second number is the one that matters, because a wrong-reason failure was round 3's worst finding, and it is now measured at zero across the whole corpus. It also confirmed the deletions are **permanent rather than dormant** — `stripScanComments`, `stripJsoncComments` and `stripDeadFalseBranches` are absent by grep, so **B12/B13/B17/B20 cannot recur because the mechanism no longer exists**, not because today's inputs happen not to trigger it — and re-attacked the R13 rewrite with the exact fixtures that defeated the regex version (decoy `1501`, decoy string literal quoting the call, genuine `if (false)` dead branch), catching all three for the right reason. **All three factual claims in the coverage-boundary comment were tested and hold**, including the uncomfortable one: it planted a real config inside a symlinked directory and confirmed the guard genuinely cannot see it, so the disclosure is honest rather than decorative; and it drove the class-name cross-check **in both directions**, `export` removed from the code and `class_name` renamed in the config only, with the dry run failing loudly each time. **Three bugs, all in test code; the configuration has never had a defect in five rounds.** **B22 MEDIUM, reproduced by the harness independently** — `index.test.ts` calls `fetch()` with **no arguments**, a shape the Durable Object runtime never produces, so a `PlanCoordinator` returning **200 to every real invocation** passes all three tests. Nothing is broken today (the shipped signature takes no parameter at all), but Evidence row 13 claims `unit` proof of *"the stub's 501"* and the test establishes it only under a call that never happens — and nothing else covers it, since the dry run proves the class is *exported*, never that invoking it returns 501, and validation-04's Miniflare probe was a one-off artifact rather than a standing guard. **B23 and B24 were both reported MEDIUM and I downgraded both, with the reasons on the record.** B23 — an array-of-tables `vars` (`[[vars]]`) parses to an **array**, and property access on an array returns `undefined`, indistinguishable from "no such key"; verified directly against `smol-toml`. Downgraded to **LOW** because an array-form `vars` **is not a deployable wrangler configuration** — wrangler rejects it — so the shape cannot ship the bypass R11 exists to prevent, and R9 catches the same malformation independently in the one file that *is* deployed. What fails is the evidence, not the property: the same calibration applied to B7 and B8. Fixed anyway, with a **type guard rather than a parser**, because a structural check that silently accepts a shape it cannot read is the exact category this task has been about. B24 — an unreadable **directory** throws uncaught because `findWranglerConfigPaths` is called outside the try/catch that wraps `readWranglerConfig`, breaking the guard's own written promise of *"never an uncaught exception"*; every other unreadable case already degrades correctly, a directory is the one sibling axis that does not. Downgraded to **LOW** because git does not track directory permissions so it cannot arrive through a commit, and when it fires CI still goes red — loudly, just misdirected. **Four things rejected in writing.** **R13's "class body" wording clashes with idiomatic TypeScript** — a doc comment precedes the declaration it documents rather than sitting inside a two-line method, so **no correct implementation satisfies those words literally**; the sign-off lens recommended recording it rather than reopening design and **I agree**, since the intent is met and `"Phase 3"` appears exactly once, in the right place. `R2` and `B10` both firing on a `workers_dev` mutation is keyword-filter over-triggering on a real defect, never staying green on one. The symlinked-directory gap stays open deliberately — closing it means following directory symlinks and reintroducing loop risk. And a long list the adversarial lens cleared is recorded so it is not re-attacked: symlink chains, ELOOP pairs, FIFO targets, BOM and binary files, empty files, no-read-permission *files*, inline tables, dotted keys, quoted headers, `vars` as a string or null, plus confirmation that `index.test.ts` is not cwd-sensitive. **One contract accuracy fix applied**, within Amendment 1's existing scope rather than as a new amendment: Evidence row 13's *Where* column never mentioned the Phase-3 assertion fix 03 added, so **the contract described less than the code proves**. Corrected, with the wording-looseness noted underneath for whoever amends it next.
- `2026-07-26` · **T04** · `bugs-found → validated` · [`work/T04-wrangler-config/fix-04.md`](work/T04-wrangler-config/fix-04.md) · **closed on the user's explicit decision, and the reasoning for stopping matters more than the fixes.** Asked what was actually looping, the answer was that **rounds 1–3's cause is genuinely dead** — the regex-over-TypeScript family, verified by the regression lens as gone by grep rather than dormant — and what replaced it is **not a defect but a scope problem**: R11 is written as a universal claim over every file in the repository, so a hostile reader can always produce one more legal-but-unhandled shape, and that has no natural end. The severity trend showed it: round 3 was 4 MED/2 LOW of regexes **misreading real code**; round 5 was 1 MED/2 LOW of shapes that **cannot be deployed at all** (wrangler rejects an array-form `vars`) or **cannot arrive through a commit** (git does not track directory permissions). The user chose to fix the one load-bearing finding and close. **Scope exceeded the authorisation and that is on the record rather than buried:** the decision was B22 only, with B23/B24 to be accepted as named boundary limits, but the fix agent had already written all three before the run was interrupted and the work was kept rather than reverted — a deliberate choice, since reverting verified working code to match a narrower instruction destroys value for tidiness. **B22** was the one that mattered: `index.test.ts` called `fetch()` with no arguments, a shape the Durable Object runtime never produces, so a stub returning **200 to every real invocation** passed; it now asserts the 501 both with no argument and with a real `Request`, via a narrow cast whose whole point is to prove the stub *ignores* an argument its parameterless signature cannot express. **B23** type-guards `vars` — an array, string, number or null is now a named violation instead of something property access silently reads as `undefined`, applying fix 03's principle that **what this scan cannot read must never be mistaken for something that sets nothing**; a type guard, not a parser. **B24** catches a `readdirSync` failure, names the unreadable directory, and **continues the walk** — the harness verified the distinguishing property directly, that a real violation in a sibling directory is still reported rather than discarded. Six probes re-run against the real tree, all correct, both checksums intact. **One honest deviation, stated rather than glossed: no fresh-agent validation ran against fix 04**, because a third full validation round is exactly what the instruction declined; what stands behind it is validation-05's two clean lenses, its 28-mutation corpus at zero-green and zero-wrong-reason, and the harness reproducing every fix fail-then-pass itself. **Final tally across six attempts: 25 findings, every single one in test code, not one in `wrangler.toml` or `src/index.ts`** — both byte-identical to attempt 1 and independently signed off four times.
- `2026-07-26` · **T04** · `validated → done` · `doc.md` + `feature.md` · **T04 delivered no user-facing feature and gets no entry in `feature.md`'s feature list**, which is the rule that file lives under — it records what a person can **do**, and nobody can do anything new. What it did deliver is five **enforced invariants**, and those belong in the file because they are the only thing this repository currently *does*: the Worker cannot be configured out from behind Access (`workers_dev` false everywhere, `run_worker_first` the boolean `true` and never the array form that would let unlisted paths bypass the Worker entirely — both measured, with the table carried into the config as a comment); no wrangler configuration anywhere in the tree can set `ENVIRONMENT` outside `[env.local]`, with a config the scan **cannot read** — unparseable, unreadable directory, a `vars` that is not a table — being itself a named violation rather than silently treated as one that sets nothing; the `[env.local]` bindings cannot drift from the top-level ones, without which the one environment that can run locally is the one with no database; the stub cannot stop falling through to the assets binding, proven by **invoking** it both the way the test likes and the way the Durable Object runtime actually calls it; and no §10.3 secret name, no `[limits]` block and no `new_classes` can enter the config. **`doc.md` gained the two new files and lost a lie** — the Stack section still said *"no `wrangler.toml` exists (T04)"* and that D1 had no binding. **Running it** now carries the two dry-run commands with the ordering that makes them work: `pnpm build` first, because wrangler hard-errors naming `assets.directory` without `apps/web/dist`, and `--env=""` rather than a bare dry run because a named environment makes wrangler warn that no target was specified. Both entries carry the instruction that outlives them: `index.ts`'s stub class stays until Phase 3 while T07 replaces only the fetch handler, `index.test.ts` is **rewritten by T07 rather than deleted**, and the guard's coverage-boundary comment — measured across six attempts — is not to be removed. **Phase 0 is now 5/21 done.**
